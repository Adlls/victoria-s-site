<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    {{>headInclude}}
  </head>
  <body>  
      <div id="app"></div>
      {{>header}}
      {{>consultation}}
      {{>about}}
      {{>workshops}}
      {{>contacts}}
      {{>scripts}}
  </body>
</html>
