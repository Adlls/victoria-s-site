<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    {{>headInclude}}
  </head>
  <body>  
      <div class="preloader">
        <div class="preloader__row">
          <div class="preloader__item"></div>
          <div class="preloader__item"></div>
        </div>
      </div>
      {{>popup}}
      {{>header}}
      {{>consultation}}
      {{>about}}
      {{>workshops}}
      {{>feedback}}
      {{>contacts}}
      {{>scripts}}
  </body>
</html>
