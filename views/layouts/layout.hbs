<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    {{>headInclude}}
  </head>
  <body>  
      <div id="app"></div>
      {{>header}}
      {{>consultation}}
      {{>about}}
      {{>individualProgram}}
      {{>workshops}}
      {{>contacts}}
      {{>scripts}}
  </body>
</html>
