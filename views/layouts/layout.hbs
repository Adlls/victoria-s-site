<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    {{>headInclude}}
  </head>
  <body>  
      {{>header}}
      {{>consultation}}
      {{>about}}
      {{>individualProgram}}
      {{>workshops}}
      {{>contacts}}
  </body>
</html>
