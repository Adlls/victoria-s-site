<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    {{>headInclude}}
  </head>
  <body class="">  
      {{>menu}}
      {{>header}}
      {{>consultation}}
      {{>about}}
      {{>individualProgram}}
      {{>footer}}
  </body>
</html>
